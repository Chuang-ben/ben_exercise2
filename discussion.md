# Step 5: Discussion

## 分析方法比較

我們進行了兩種不同的座標系統分析：

### 1. 假設分析 (crs_analysis.py)
**方法**: 將 TWD67 座標直接當作 WGS84 處理
**目的**: 演示錯誤使用座標系統的影響

### 2. 正確轉換分析 (correct_crs_conversion.py)  
**方法**: 使用 pyproj 正確轉換 TWD67 (EPSG:3824) 到 WGS84 (EPSG:4326)
**目的**: 評估正確座標轉換的精度

---

## What does the offset look like visually?
若將整個台灣拉入視窗則幾乎看不出位移差距，不過只要稍微放大到縣市等級就能明顯看出兩者的位移差異。

## What was the average distance?
**假設分析結果**: 平均而言，直接將 TWD67 當作 WGS84 會產生約 0.85 公里的定位誤差。

**正確轉換結果**: 使用 pyproj 正確轉換後，API 提供的 TWD67 與 WGS84 座標之間仍有約 0.851 公里的系統性差異。

## In a disaster scenario, what could this offset cause?
在緊急情況下，若將 TWD67 座標誤當作 WGS84 使用，將產生顯著的定位誤差，平均偏移約 0.85 公里。此誤差可能導致救援人員誤入災區錯誤位置，或無法即時取得對應的災害資訊與受困人員位置，進而延誤救援時效。

## 為什麼 API 同時保留兩種坐標系統？如果 WGS84 是全球標準，為什麼不直接全部轉換就好？
TWD67 是台灣本土的座標系統，WGS84 是全球通用的座標系統。而許多早期資料都是用 TWD67 建立的，若直接轉換可能會對資料造成影響以及法律上的爭議，為保持資料一致性與可追溯性，因此同時保留兩套系統。

## 關鍵洞察

1. **座標系統重要性**: 正確的座標轉換對 GIS 應用至關重要
2. **系統性差異**: 即使正確轉換，API 中的兩種座標仍存在約 0.85 公里的差異
3. **實務應用**: 在災害應用中，這種差異可能影響救援效率
4. **資料完整性**: 保留多種座標系統有助於資料追溯和驗證